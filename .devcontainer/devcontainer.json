{
    "name": "Shaheen",
    "dockerFile": "Dockerfile",
    "appPort": [],
    "extensions": [],
    "runArgs": [
      "--net","host",
    ],
    "settings": {
        "terminal.integrated.shell.linux": "/usr/bin/fish"
    },    
    "postCreateCommand": "npm install && cd client && yarn install && yarn build"
}
